# Globalcompany_sales
End-to-end data pipeline: raw CSV → Python ETL → SQLite → SQL metrics → Excel/Power BI BI. Calculates revenue/profit by region &amp; item type + quarterly trend. Shows real data engineering + analysis, not just a dashboard. PBIX + queries + scripts included.
